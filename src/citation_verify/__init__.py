"""citation-verify — dual-channel verification for AI-hallucinated citations.

The package is the canonical import path per PLAN.md Phase 0. Submodules are
imported on demand to keep the package import lightweight (e.g. for the
schema-only tests that don't need the runtime stack).
"""
